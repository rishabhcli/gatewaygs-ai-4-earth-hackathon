# Regenerable evidence

Only outputs produced by committed commands from immutable or versioned inputs
belong here. Every artifact must state its generating command, tool versions,
input identifiers or checksums, and a `seed` key. Deterministic generators use
`null`; randomized generators record the exact seed.

Hand-written screenshots, copied terminal output, and unversioned provider
responses are not evidence.
